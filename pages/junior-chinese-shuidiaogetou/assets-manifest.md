# 素材清单 — 《水调歌头·明月几时有》

## 图片 / 音频

| 页 | 素材 | 来源 |
|----|------|------|
| 全部 | 无外链图片、无音频 | 一律 CSS/SVG 自绘 |

- 封面夜空、明月、云：CSS 渐变 + `radial-gradient` + `box-shadow` 绘制，**不使用外链图或 base64**。
- 各页装饰（月相、印章感标签、分隔线）：CSS 绘制。

## 字体

- preset：`serif-classic`，**fallback-only**（系统宋体栈 `"Noto Serif SC","Songti SC","STSong",serif`；注释用楷体栈）。
- 不写 `@font-face`、不引用外链字体 URL。

## 文本

- 词作原文、小序、注释均为公有领域（苏轼，北宋），直接嵌入 HTML。
