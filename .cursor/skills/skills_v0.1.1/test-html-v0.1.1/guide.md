# 本地 HTML 验收指南

## must-cover

must-cover 来自四类信息：

| 来源 | 检查方式 |
|---|---|
| core-loop | 端到端触发，并看到状态或反馈变化 |
| require | 必含页面、元素、素材或互动全部出现 |
| forbid | 禁止项全局不存在 |
| delivery | 页面能通过本地 HTTP 服务打开 |

多页课件额外检查：

| 项 | 检查方式 |
|---|---|
| 壳加载 | 页面出现缩略图栏和主预览 |
| 页数 | `template.page-data` 数量等于 outline 页数 |
| 翻页 | 点击和键盘翻页可用 |
| 状态恢复 | 互动页操作后离开再返回，状态仍在 |
| 分数消息 | 有练习页时，完成判分后能发送 `cwScore` |
| 打包入口 | 如果页面提供 SCORM 包按钮，按钮存在且点击后不报错 |

## 人工验收

1. 在产物目录启动本地 HTTP 服务。
2. 打开 `http://127.0.0.1:<port>/index.html`。
3. 按 must-cover 逐项记录通过或失败。
4. 失败项写清楚页面现象和回修位置。

## 本地 Playwright 验收

需要自动化时，用本地 Playwright 打开本地 URL。脚本至少覆盖：

- 标题或主容器可见；
- 没有明显水平溢出；
- 核心按钮点击后有状态变化；
- 答题或拖拽任务有提交、反馈和重置；
- 多页课件能翻页；
- 练习页能触发 `cwScore` message。
- 如果有 SCORM 包按钮，点击后没有页面级错误。

不要只验证“元素存在”。核心互动必须真的走完。

## 失败处理

默认先判断页面实现是否缺失。只有选择器错误、脚本语法错误、或诊断确认页面正确时，才修改测试脚本。连续失败时先打印 DOM 状态或手动复现，不要放宽验收项。
